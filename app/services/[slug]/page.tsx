import Link from 'next/link';
import { notFound } from 'next/navigation';
import { business, services, cities } from '../../data';

type Props={params:Promise<{slug:string}>};
export async function generateStaticParams(){return services.map(s=>({slug:s.slug}));}
export async function generateMetadata({params}:Props){const {slug}=await params; const s=services.find(x=>x.slug===slug); if(!s)return{}; return {title:`${s.title} in Silver Spring, MD`,description:`${s.title} from Arsha Electric.co. Serving Silver Spring, MD and the DMV. ${s.short}`};}
export default async function ServicePage({params}:Props){const {slug}=await params; const s=services.find(x=>x.slug===slug); if(!s)notFound(); const schema={"@context":"https://schema.org","@type":"Service","name":s.title,"description":s.short,"provider":{"@type":"Electrician","name":business.name,"telephone":business.phone},"areaServed":cities.map(c=>`${c.name}, ${c.state}`)}; return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="page-hero"><div className="container"><div className="crumb">Service</div><h1>{s.title} in Silver Spring, MD</h1><p className="lead">{s.short} Serving homeowners and small businesses across the DMV.</p></div></section><section className="section"><div className="container two-col"><article><h2>Local {s.title.toLowerCase()} service</h2><p>Arsha Electric.co is built around clean workmanship, clear communication, and modern electrical solutions. This page is optimized for people searching for {s.keywords} in Silver Spring, Montgomery County, Washington DC, and Northern Virginia.</p><h3>Common requests</h3><div className="check"><div>Diagnose the issue and explain the safest repair path.</div><div>Install or replace devices with neat, professional finishes.</div><div>Recommend upgrade options when smart home, low-voltage, or higher-capacity solutions make sense.</div></div><h3>Best next step</h3><p>Send a short description, location, and photos. For urgent issues like burning smells, sparking, heat at devices, or repeated breaker trips, stop using the affected circuit and call a licensed electrician.</p></article><aside className="sidebar card"><h3>Request service</h3><p>{business.phone}</p><p>{business.email}</p><Link className="btn" href="/contact">Contact Arsha</Link></aside></div></section></main>}
